export default function EmptyState() {
    return (
        // Empty state - would be shown when no items
        <div className="empty-state">
          <p>No grocery items added yet. Start by adding your first item!</p>
        </div>
    )
}